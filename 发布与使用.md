# 发布与使用

## 发布到 GitHub Pages

1. 把整个目录提交并推送到仓库的 `main` 分支。
2. 在 GitHub 仓库打开 **Settings → Pages**。
3. 将 **Build and deployment → Source** 设为 **GitHub Actions**。
4. 推送后，`Deploy Owlbear extensions` 会自动运行；也可在 **Actions** 页面点 **Run workflow** 手动重跑。
5. 等两个任务都显示绿色后再安装扩展。

本仓库用户名与仓库名对应的清单地址：

```text
https://sixcoins233.github.io/owlbear-gm-tools/normal-d20/manifest.json
https://sixcoins233.github.io/owlbear-gm-tools/quest-board/manifest.json
https://sixcoins233.github.io/owlbear-gm-tools/destiny-dice/manifest.json
https://sixcoins233.github.io/owlbear-gm-tools/bilibili-music/manifest.json
```

在 Owlbear Rodeo 个人资料中选择 **Add Extension**，粘贴需要的清单地址，然后在房间设置中启用扩展。

## Bilibili Music

1. 每位用户进入房间后都会看到播放器小窗；小窗可拖动、收起或隐藏。
2. GM 打开插件，粘贴包含 `BV...` 的完整视频链接或直接输入 BV 号，可选填显示名称。
3. GM 在歌单中点击播放按钮，然后使用暂停、继续、停止、上一首和下一首。
4. 点击歌单中的 `☆` 收藏；收藏仅保存在 GM 当前 Chrome，可随时重新加入歌单。
5. 玩家只能查看房间歌单。每个人的音量、播放器位置、收起和隐藏状态互不影响。

### Chrome 设置

- 首次播放若没有声音，请展开小窗并点击一次 Bilibili 播放器，随后解除静音。Chrome 通常不允许未交互页面直接有声自动播放。
- 如果仍无声音，打开地址栏左侧的网站设置，确认 `www.owlbear.rodeo` 的**声音**权限为允许；也检查 Chrome 的 **设置 → 隐私和安全 → 网站设置 → 更多内容设置 → 声音**。
- 若 Bilibili 登录态或受限视频无法加载，可仅对 Owlbear/Bilibili 放行第三方 Cookie。普通公开投稿通常不需要此项。
- 不需要摄像头、麦克风、弹窗或下载权限。插件清单只申请 Owlbear 的 `autoplay` 权限，并在内嵌播放器上委托自动播放。

由于 Bilibili 普通投稿播放器没有公开完整的跨域控制接口，本插件保留官方播放器。插件音量滑条对播放器进行兼容控制；若某个视频无效，请使用播放器右下角的原生音量按钮。插件不会提取、代理或下载视频音轨。

## 更新

修改后提升对应 `public/manifest.json` 的版本号，再推送到 `main`。等待 Actions 变绿；若 Owlbear 仍显示旧版本，先直接打开清单 URL 检查版本，再在扩展设置中重新加载或删除后重新添加。
