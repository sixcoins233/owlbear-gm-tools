# 发布与使用

## 一次性免费发布到 GitHub Pages

1. 在 GitHub 新建一个 **Public** 仓库，例如 `owlbear-gm-tools`，不要勾选自动创建 README。
2. 把当前整个目录提交并推送到该仓库的 `main` 分支。
3. 打开仓库的 **Settings → Pages**，在 **Build and deployment → Source** 选择 **GitHub Actions**。
4. 打开仓库的 **Actions** 页，等待 `Deploy Owlbear extensions` 变为绿色。
5. 发布地址会是 `https://你的用户名.github.io/仓库名/`。

本仓库已包含 `.github/workflows/pages.yml`，每次推送到 `main` 都会自动构建并重新发布两个插件。

## 添加到 Owlbear Rodeo

在 Owlbear Rodeo 的个人资料页选择 **Add Extension**，分别粘贴：

```text
https://你的用户名.github.io/仓库名/normal-d20/manifest.json
https://你的用户名.github.io/仓库名/quest-board/manifest.json
```

创建新房间时勾选这两个扩展。已有房间则进入房间设置启用扩展；房间内所有成员使用同一份房间数据。

## 正态 d20 使用

1. GM 点击左上角紫色 d20 图标。
2. 在 **GM 设置** 中勾选允许投掷的在线玩家，并选择是否公开结果。
3. 获准用户点击图标，调整中心值、分布宽度 σ 和加值，再点击 **投掷 d20**。
4. 隐藏投掷只显示给投掷者和 GM；GM 可在 **待公开的隐藏结果** 中点击 **公开**。

## 任务栏使用

1. 启用插件后，任务栏会自动出现在每位用户屏幕左侧。
2. GM 点击每类右侧 `＋` 添加任务，直接编辑文字，使用复选框修改完成状态。
3. 玩家使用顶部复选框切换“显示已完成任务/仅未完成任务”。
4. 点击分类左侧三角展开或收起；拖动标题栏移动面板，拖动右下角手柄调整宽高。
5. 使用标题栏右上角的字号框选择小、标准、大或特大文字。大小、位置和字号均按用户单独保存。
6. 点击面板右上角 `×` 隐藏。要再次显示或重置位置，点击 Owlbear 左上角绿色任务栏图标。

## 更新

修改代码后推送到 `main`，GitHub Actions 会自动发布。若 Owlbear 缓存了旧清单，请提升对应 `public/manifest.json` 的 `version`，然后在个人资料的扩展设置中重新加载。
