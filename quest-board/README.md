# 悬浮任务栏

## 功能

- 固定顺序：主线任务、支线任务、角色任务。
- GM 可添加、编辑、删除任务，并勾选完成/未完成。
- 玩家可选择显示全部任务或只显示未完成任务。
- 每一类都可独立展开/收起。
- 拖动标题栏移动；位置、筛选和折叠状态按用户保存在浏览器本地。
- 任务数据保存在 Owlbear 房间元数据并实时同步。

点击 Owlbear 左上角的任务栏图标可隐藏、重新显示或重置位置。悬浮栏右上角的 `×` 也可隐藏。

Owlbear 房间元数据总量限制为 16 KB，因此建议任务描述保持简短；本插件也把每条描述限制为 500 个字符。
